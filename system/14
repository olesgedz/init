# Run the ‘tail -f /var/log/syslog‘ command in background
sudo tail -f /var/log/syslog &
# It's also possible to launch the command, then do Ctrl + Z (which stop it) and then type "bg" to put it in the background